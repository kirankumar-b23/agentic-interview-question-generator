openai>=1.30.0
pydantic>=2.0.0
pandas>=2.0.0
networkx>=3.0
scikit-learn>=1.3.0
# Imported DIRECTLY by src/embeddings.py and src/question_bank.py, so it is declared rather than
# relied on as a transitive dependency of pandas/scikit-learn.
numpy>=1.24
# scripts/ingest_xlsx_questions.py reads data/raw/*.xlsx with openpyxl (deliberately not pandas).
# It was undeclared, so a fresh install hit ImportError on the one script that needs it.
openpyxl>=3.1
requests>=2.31.0
gspread>=6.0.0
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pytest>=8.0.0
httpx>=0.27.0            # FastAPI TestClient (tests/test_api.py)
tavily-python>=0.5
# Local semantic embeddings (free, no API key) for retrieval/MMR/coverage/attribution.
# Falls back to TF-IDF if unavailable. Install CPU torch to keep it lean:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=2.2.0
